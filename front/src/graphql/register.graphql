mutation Register(
  $surname: String!
  $name: String!
  $password: String!
  $email: String!
) {
  register(surname: $surname, name: $name, password: $password, email: $email) {
    accessToken
  }
}
